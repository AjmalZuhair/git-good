# Rebase Practice (Branch rebase-b)

Instructions:
1. Checkout `rebase-b`: `git checkout rebase-b`
2. Run `git rebase rebase-a`
3. The rebase should succeed without conflicts because each branch adds different files.
4. Confirm that both files are present after the rebase.
